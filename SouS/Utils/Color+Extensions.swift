import SwiftUI

extension Color {
    // Define a custom blue color similar to the screenshots
    // Values are approximate - adjust RGB as needed for exact match
    static let primaryBlue = Color(red: 0.2, green: 0.4, blue: 0.9)
    
    // Add other custom colors here if needed
    // static let lightGrayBackground = Color(UIColor.systemGray6)
} 